import 'mark8_2.dart';
import 'mark8_3.dart';

void main() {
  // object of square class
  var sq = sqaure(side: 10);
  print(sq.getArea());
//  odject of cube class
  var cub = cube(side: 25);
  print(cub.getArea());
  print(cub.getVol());
}
