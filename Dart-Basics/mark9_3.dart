import 'mark9_2.dart';

void main() {
  // creating an object for interface class which implements 2 abstruct class
  var shope1 = shope(price: 10.0, name: "batman");
  print(shope1.price);
  print(shope1.name);
}
